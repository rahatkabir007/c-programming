#include <stdio.h>
int main()

{
    int a = 10, b = 20;
    // int ans = a + b;
    // int ans = a - b;
    // int ans = a * b;
    // int ans = b / a;
    int mod = a / b;
    // printf("%d", ans);
    printf("%d", mod);
    return 0;
}